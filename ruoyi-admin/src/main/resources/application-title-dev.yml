spring:
  datasource:
    dynamic:
      p6spy: false
      datasource:
        master:
          url: jdbc:mysql://localhost:3306/ry-vue?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
          username: root
          password: root
  data:
    redis:
      host: localhost
      port: 6379
      password: ruoyi123
tenant:
  enable: false
captcha:
  enable: false
api-decrypt:
  enabled: false
spring.boot.admin.client:
  enabled: false
snail-job:
  enabled: false
  # ruoyi-job 的演示 Bean 即使禁用客户端仍会解析该占位符。
  port: 2${server.port}
mail:
  enabled: false
sms:
  restricted: true
